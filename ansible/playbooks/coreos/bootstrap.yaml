---
- name: Bootstrap CoreOS VM
  hosts: homeserver_cluster
  become: true
  vars:
    ansible_user: ansible
  roles:
    - setup_user
    - coreos_tuning

- name: Deploy k3s cluster
  ansible.builtin.import_playbook: k3s_deploy.yaml
