---
# Single entry point: run this to bootstrap or reconfigure everything.
# Uses import_playbook so each playbook runs sequentially.

- import_playbook: coreos/bootstrap.yaml

- import_playbook: coreos/k3s_deploy.yaml

- import_playbook: talos/bootstrap.yaml

- import_playbook: truenas/setup_host.yaml
