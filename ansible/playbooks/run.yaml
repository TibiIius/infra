---
# Idempotent configuration: run this to ensure all servers are in correct state.
# No Terraform, no ISO generation, no pauses.

- import_playbook: coreos/configure.yaml

- import_playbook: coreos/k3s_deploy.yaml

- import_playbook: truenas/setup_host.yaml
